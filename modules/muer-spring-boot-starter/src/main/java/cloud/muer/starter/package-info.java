/**
 * Spring Boot starter entry point for Muer.
 *
 * <p>This module intentionally contains no runtime implementation classes. It aggregates the
 * autoconfiguration and Spring Boot dependencies required by applications that consume Muer.
 * The package descriptor exists so release builds can produce valid source and Javadoc artifacts
 * for Maven Central.</p>
 *
 * @since 0.1.0
 */
package cloud.muer.starter;
