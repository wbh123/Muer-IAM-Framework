<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wust.iam.persistence.mapper.IamSessionMapper">
    <resultMap id="authSession" type="com.wust.iam.session.AuthSession">
        <constructor>
            <arg column="session_id" javaType="java.lang.String"/>
            <arg column="user_id" javaType="_long"/>
            <arg column="client_type" javaType="java.lang.String"/>
            <arg column="client_instance" javaType="java.lang.String"/>
            <arg column="ip_address" javaType="java.lang.String"/>
            <arg column="user_agent" javaType="java.lang.String"/>
            <arg column="login_at" javaType="java.time.Instant"/>
            <arg column="last_seen_at" javaType="java.time.Instant"/>
            <arg column="expires_at" javaType="java.time.Instant"/>
            <arg column="revoked_at" javaType="java.time.Instant"/>
            <arg column="logout_at" javaType="java.time.Instant"/>
            <arg column="revoke_reason" javaType="java.lang.String"/>
        </constructor>
    </resultMap>

    <sql id="sessionColumns">
        session_id, user_id, client_type, client_instance, ip_address, user_agent, login_at, last_seen_at,
        expires_at, revoked_at, logout_at, revoke_reason
    </sql>

    <select id="findById" resultMap="authSession">
        SELECT <include refid="sessionColumns"/>
        FROM iam_session
        WHERE session_id = #{sessionId}
    </select>

    <select id="findActiveForUser" resultMap="authSession">
        SELECT <include refid="sessionColumns"/>
        FROM iam_session
        WHERE user_id = #{userId}
          AND revoked_at IS NULL
          AND expires_at &gt; CURRENT_TIMESTAMP(6)
        ORDER BY login_at DESC, session_id
    </select>

    <insert id="insert">
        INSERT INTO iam_session
            (session_id, user_id, client_type, client_instance, ip_address, user_agent, login_at, last_seen_at,
             expires_at, revoked_at, logout_at, revoke_reason)
        VALUES
            (#{sessionId}, #{userId}, #{clientType}, #{clientInstance}, #{ipAddress}, #{userAgent},
             #{loginAt}, #{lastSeenAt}, #{expiresAt}, #{revokedAt}, #{logoutAt}, #{revokeReason})
    </insert>

    <update id="update">
        UPDATE iam_session
        SET client_type = #{clientType},
            client_instance = #{clientInstance},
            ip_address = #{ipAddress},
            user_agent = #{userAgent},
            last_seen_at = #{lastSeenAt},
            expires_at = #{expiresAt},
            revoked_at = #{revokedAt},
            logout_at = #{logoutAt},
            revoke_reason = #{revokeReason}
        WHERE session_id = #{sessionId}
    </update>

    <update id="touch">
        UPDATE iam_session
        SET last_seen_at = #{lastSeenAt}
        WHERE session_id = #{sessionId}
          AND revoked_at IS NULL
          AND expires_at &gt; #{lastSeenAt}
    </update>
</mapper>
