<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>io.github.muer</groupId>
    <artifactId>muer-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Muer IAM Framework</name>
    <description>Identity infrastructure that grows naturally with your applications.</description>
    <url>https://muer.github.io</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <scm>
        <connection>scm:git:https://github.com/wbh123/iam.git</connection>
        <developerConnection>scm:git:git@github.com:wbh123/iam.git</developerConnection>
        <url>https://github.com/wbh123/iam</url>
        <tag>HEAD</tag>
    </scm>

    <developers>
        <developer>
            <id>wbh123</id>
            <name>Muer maintainer</name>
            <url>https://github.com/wbh123</url>
        </developer>
    </developers>

    <modules>
        <module>muer-core</module>
        <module>muer-authentication</module>
        <module>muer-authorization</module>
        <module>muer-session</module>
        <module>muer-audit</module>
        <module>muer-diagnostics</module>
        <module>muer-persistence-mybatis</module>
        <module>muer-management-web</module>
        <module>muer-spring-boot-autoconfigure</module>
        <module>muer-spring-boot-starter</module>
        <module>muer-example</module>
        <module>muer-tests</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-boot.version>4.0.0</spring-boot.version>
        <maven-surefire-plugin.version>3.5.4</maven-surefire-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration><release>${maven.compiler.release}</release></configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
